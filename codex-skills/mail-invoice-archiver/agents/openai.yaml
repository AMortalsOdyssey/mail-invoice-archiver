interface:
  display_name: "Mail Invoice Archiver"
  short_description: "Organize invoices downloaded from webmail"
  default_prompt: "Use $mail-invoice-archiver to collect invoices from the mailbox web UI, import downloaded invoice files, deduplicate them, and prepare monthly summaries."
